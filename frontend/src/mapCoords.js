// مختصات هات‌اسپات‌ها روی نقشهٔ وستروس، به‌صورت درصد از عرض/ارتفاع تصویر (0 تا 100)
// نقشه با westros1.webp جایگزین شد (۲۰۲۶/۰۷) — نسبتِ عرض‌به‌ارتفاعِ این فایل خیلی
// فرق داره با نسخه‌های قبلی (۰.۴۵ در برابر ۰.۷۲/۰.۷۹، کادربندیِ کاملاً متفاوت)، پس
// همهٔ پین‌های ازقبل‌ثبت‌شده جابه‌جا به‌نظر می‌رسند تا از پنل ادمین دوباره دقیق گذاشته شوند
export const MAP_IMAGE = '/maps/westros1.webp';

// مختصات قلعه‌های شمال قبلاً همین‌جا هاردکد بودند و مستقل از دیتابیس رندر می‌شدند —
// یعنی روی نقشه دیده می‌شدند ولی ادمین هیچ راهی برای حذف/ادیتشان نداشت (چون آن بخش
// فقط قلعه‌های واقعاً ثبت‌شده در map_castles را لیست می‌کند). این دیتا حالا توی
// backend/main.py (_seed_north_map_castles) یک‌بار به‌صورت خودکار توی map_castles
// می‌شینه، پس این‌جا دیگه لازم نیست — از همون مسیرِ عادیِ ادمین قابل‌حذف/ادیت است.

// کراپ نمایشی: چون خود اقلیم شمال فقط در قسمتِ بالای تصویر جا می‌شه، برای زوم بهتر
// این بخش رو با یک ویوباکس محدود نشون می‌دیم (درصد از تصویر کامل) — این عددها تخمینیِ
// اولیه‌ان روی نقشهٔ جدید (westros1.webp)، ممکنه لازم باشه دقیق‌تر تنظیم بشن
export const REGION_VIEWBOX = {
  north: { x: 0, y: 0, w: 100, h: 40 },
};
